@model IEnumerable<TFMGoSki.Dtos.ReservationTimeRangeMaterialDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RemainingMaterialsQuantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartDateOnly)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndDateOnly)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartTimeOnly)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndTimeOnly)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RemainingMaterialsQuantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDateOnly)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndDateOnly)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartTimeOnly)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndTimeOnly)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
